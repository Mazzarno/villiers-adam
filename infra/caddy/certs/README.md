Place Cloudflare Origin Certificate files in this directory before starting the preprod reverse proxy.

Expected filenames:

- `cloudflare-origin.pem`
- `cloudflare-origin.key`

Do not commit real certificate material.
